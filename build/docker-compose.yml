version: '3.7'

services:
  app_version:
    build:
      context: ../.
      dockerfile: build/version/Dockerfile
    working_dir: /app
    volumes:
      - '../.:/app'
    environment:
      HTTP_TIMEOUT_SEC: '${HTTP_TIMEOUT_SEC}'
